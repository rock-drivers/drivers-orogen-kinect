# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(kinectTaskLib)
ADD_LIBRARY(${KINECT_TASKLIB_NAME} SHARED 
    ${KINECT_TASKLIB_SOURCES})

add_dependencies(${KINECT_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${KINECT_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${KINECT_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${KINECT_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${KINECT_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${KINECT_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${KINECT_TASKLIB_HEADERS}
    DESTINATION include/orocos/kinect)

